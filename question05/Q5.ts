let famousPerson: string = ("William Shakespeares");
let message: string =(`${famousPerson} said, "Listen to many, speak to a few".`);
console.log(`${famousPerson} said, "Listen to many, speak to a few".`);